<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp">

    <android.support.v7.widget.AppCompatSpinner
        android:id="@+id/filter_server"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/bg_edit_text_selector"
        app:popupTheme="@style/AppTheme.PopupOverlay"/>

    <android.support.v7.widget.AppCompatSpinner
        android:id="@+id/filter_type"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/filter_server"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_edit_text_selector"
        app:popupTheme="@style/AppTheme.PopupOverlay"/>

    <android.support.v7.widget.AppCompatSpinner
        android:id="@+id/filter_season"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/filter_type"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_edit_text_selector"
        app:popupTheme="@style/AppTheme.PopupOverlay"/>

    <CheckBox
        android:id="@+id/filter_is_season"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/filter_season"
        android:layout_marginTop="16dp"
        android:text="@string/season_character"/>

    <CheckBox
        android:id="@+id/filter_is_hardcore"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/filter_is_season"
        android:layout_marginTop="8dp"
        android:text="@string/hardcore_character"/>

</RelativeLayout>